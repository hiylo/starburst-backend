# .secrets-allowlist — check-secrets.sh 放行清单
# 每行一个 grep -E 正则；匹配到的行不会命中检查。`#` 开头为注释。
# 只登记确属误报的模式（如检测逻辑的单测夹具），不得登记真实凭据。

# 合规检测单测中用于验证的截断假密钥（MIIB... 为截断标记，非真实密钥）
BEGIN (RSA|EC|OPENSSH|DSA|PGP) PRIVATE KEY-----\\nMIIB
